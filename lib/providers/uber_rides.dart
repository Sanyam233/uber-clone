
//UberRides object to contain properties such as type of uber, drop off time, price etc
class UberRides {

  final uberType;
  final dropoffTime;
  final price;
  bool isSelected;

  UberRides(this.uberType, this.dropoffTime, this.price, this.isSelected);
  
}